<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#d9d9ff"
    android:orientation="vertical"
    tools:viewBindingIgnore="@string/material_motion_easing_linear">

    <TextView
        android:id="@+id/texto"
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:layout_marginTop="50dp"
        android:gravity="center"
        android:text="Sufiyan's Calendar"
        android:textSize="30sp"
        tools:ignore="HardcodedText"
        android:textColor="@color/black"/>

    <TextView

        android:id="@+id/dateView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:gravity="center"
        android:textSize="40sp"
        android:textColor="@color/black"/>


    <CalendarView
        android:id="@+id/calenderView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="80dp"
        android:background="#eaeaff"
        android:theme="@style/Theme.AppCompat.Light">
    </CalendarView>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_marginLeft="7dp"
        android:layout_marginTop="30dp"
        android:layout_marginRight="7dp"
        android:gravity="center"
        android:text="“Yesterday is history, tomorrow is a mystery, today is a gift, which is why we call it the present”"
        android:textSize="15sp"
        tools:ignore="HardcodedText"
        android:textColor="@color/black"/>


</LinearLayout>